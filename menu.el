(defun laven ()
  "a nonce menu function"
  (interactive)
  (message "hei, LavenLiu"))

(defun laven-hotel ()
  "another nonce menu function"
  (interactive)
  (message "hotel, motel, holiday inn"))



